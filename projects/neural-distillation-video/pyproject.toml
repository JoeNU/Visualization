[project]
name = "neural-distillation-video"
version = "0.1.0"
description = "Procedural code for producing neon AI research explainer videos."
requires-python = ">=3.11"
dependencies = [
  "pillow>=10.4",
]

